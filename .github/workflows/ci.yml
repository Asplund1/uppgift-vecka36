name: CI

on:
  push:
    branches:
      - dev

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t asplund1/frontend:latest ./frontend
      - run: |
          npm install
          npm test

  api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t asplund1/api:latest ./api
      - run: |
          npm install
          npm test